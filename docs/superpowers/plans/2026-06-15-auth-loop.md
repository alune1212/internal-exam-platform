# 前后端身份认证闭环 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让前端 API 请求自动携带认证 header（admin 用 `X-Admin-Token`，候选人用 `X-Candidate-Id`），实现前后端身份闭环。

**Architecture:** 在 `client.ts` 的 `apiRequest`/`uploadRequest` 中根据请求路径自动注入对应 header。Admin token（密码明文）存 localStorage，候选人 ID 从已有 `candidateSession` 读取。401 时自动清 session 并跳转登录页。

**Tech Stack:** TypeScript, React, Vitest, jsdom, localStorage

---

### Task 1: 新建 `adminSession.ts`

**Files:**
- Create: `frontend/src/lib/adminSession.ts`
- Test: `frontend/src/lib/adminSession.test.ts`

- [ ] **Step 1: 编写测试**

```typescript
// frontend/src/lib/adminSession.test.ts
import { describe, it, expect, beforeEach } from "vitest";
import { getAdminToken, setAdminToken, clearAdminToken } from "./adminSession";

describe("adminSession", () => {
  beforeEach(() => {
    localStorage.clear();
  });

  it("初始状态返回 null", () => {
    expect(getAdminToken()).toBeNull();
  });

  it("set 后 get 返回相同值", () => {
    setAdminToken("my-password");
    expect(getAdminToken()).toBe("my-password");
  });

  it("clear 后 get 返回 null", () => {
    setAdminToken("my-password");
    clearAdminToken();
    expect(getAdminToken()).toBeNull();
  });

  it("覆盖写入", () => {
    setAdminToken("first");
    setAdminToken("second");
    expect(getAdminToken()).toBe("second");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd frontend && npx vitest run src/lib/adminSession.test.ts
```

预期：FAIL — `Cannot find module './adminSession'`

- [ ] **Step 3: 实现**

```typescript
// frontend/src/lib/adminSession.ts
const STORAGE_KEY = "internal-exam-admin-token";

export function getAdminToken(): string | null {
  return window.localStorage.getItem(STORAGE_KEY);
}

export function setAdminToken(token: string): void {
  window.localStorage.setItem(STORAGE_KEY, token);
}

export function clearAdminToken(): void {
  window.localStorage.removeItem(STORAGE_KEY);
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd frontend && npx vitest run src/lib/adminSession.test.ts
```

预期：4 tests PASS

- [ ] **Step 5: 提交**

```bash
git add frontend/src/lib/adminSession.ts frontend/src/lib/adminSession.test.ts
git commit -m "feat: 新增 adminSession token 管理"
```

---

### Task 2: 修改 `client.ts` — 自动注入 auth headers + 401 处理

**Files:**
- Modify: `frontend/src/api/client.ts`
- Test: `frontend/src/api/client.test.ts`

- [ ] **Step 1: 编写测试**

```typescript
// frontend/src/api/client.test.ts
import { describe, it, expect, beforeEach, vi } from "vitest";
import { apiRequest, ApiError } from "./client";
import { setAdminToken, clearAdminToken } from "@/lib/adminSession";
import { setCurrentCandidate, clearCurrentCandidate } from "@/lib/candidateSession";
import type { Candidate } from "@/types/candidate";

const mockCandidate: Candidate = {
  id: 42,
  name: "张三",
  employee_no: "E001",
  department: "技术部",
  status: "active",
};

function mockFetchJson(data: unknown, status = 200) {
  vi.stubGlobal(
    "fetch",
    vi.fn().mockResolvedValue({
      ok: status >= 200 && status < 300,
      status,
      json: () =>
        Promise.resolve(
          status >= 200 && status < 300
            ? { success: true, data, message: "ok" }
            : { detail: "error" },
        ),
    }),
  );
}

describe("apiRequest auth headers", () => {
  beforeEach(() => {
    localStorage.clear();
    vi.restoreAllMocks();
  });

  it("admin 路径自动带 X-Admin-Token", async () => {
    setAdminToken("admin-pass");
    mockFetchJson([]);
    await apiRequest("/api/admin/exams");
    const [, init] = vi.mocked(fetch).mock.calls[0];
    expect(init?.headers).toMatchObject({ "X-Admin-Token": "admin-pass" });
  });

  it("admin 路径无 token 时不带 header", async () => {
    clearAdminToken();
    mockFetchJson([]);
    await apiRequest("/api/admin/exams");
    const [, init] = vi.mocked(fetch).mock.calls[0];
    expect(init?.headers).not.toHaveProperty("X-Admin-Token");
  });

  it("候选人路径自动带 X-Candidate-Id", async () => {
    setCurrentCandidate(mockCandidate);
    mockFetchJson({ attempt_id: 1 });
    await apiRequest("/api/exams/1/start", { method: "POST" });
    const [, init] = vi.mocked(fetch).mock.calls[0];
    expect(init?.headers).toMatchObject({ "X-Candidate-Id": "42" });
  });

  it("公开路径无候选人时不带 candidate header", async () => {
    clearCurrentCandidate();
    mockFetchJson([]);
    await apiRequest("/api/exams/active");
    const [, init] = vi.mocked(fetch).mock.calls[0];
    expect(init?.headers).not.toHaveProperty("X-Candidate-Id");
  });

  it("401 admin 请求清 token 并跳转", async () => {
    setAdminToken("old-pass");
    const assignSpy = vi.fn();
    vi.stubGlobal("location", { href: "", assign: assignSpy });
    mockFetchJson(null, 401);
    await expect(apiRequest("/api/admin/exams")).rejects.toBeInstanceOf(ApiError);
    expect(localStorage.getItem("internal-exam-admin-token")).toBeNull();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd frontend && npx vitest run src/api/client.test.ts
```

预期：部分 FAIL — headers 不含预期字段

- [ ] **Step 3: 实现修改**

完整替换 `frontend/src/api/client.ts`：

```typescript
import { getAdminToken, clearAdminToken } from "@/lib/adminSession";
import { getCurrentCandidate, clearCurrentCandidate } from "@/lib/candidateSession";

export type ApiResponse<T> = {
  success: boolean;
  data: T;
  message: string;
};

export class ApiError extends Error {
  status: number;
  detail?: string;

  constructor(message: string, status: number, detail?: string) {
    super(message);
    this.name = "ApiError";
    this.status = status;
    this.detail = detail;
  }
}

const API_BASE_URL = import.meta.env.VITE_API_BASE_URL ?? "";

function resolveAuthHeaders(path: string): Record<string, string> {
  if (path.includes("/api/admin/")) {
    const token = getAdminToken();
    return token ? { "X-Admin-Token": token } : {};
  }
  const candidate = getCurrentCandidate();
  if (candidate) {
    return { "X-Candidate-Id": String(candidate.id) };
  }
  return {};
}

async function parseError(response: Response): Promise<ApiError> {
  let detail: string | undefined;
  try {
    const body = (await response.json()) as { detail?: unknown; message?: unknown } | null;
    if (body && typeof body.detail === "string") {
      detail = body.detail;
    } else if (body && typeof body.message === "string") {
      detail = body.message;
    }
  } catch {
    // response body was not JSON; fall through to status-only message
  }
  const message = detail ?? `Request failed: ${response.status}`;
  return new ApiError(message, response.status, detail);
}

function handle401(path: string): void {
  if (path.includes("/api/admin/")) {
    clearAdminToken();
    window.location.href = "/admin/login";
  } else {
    clearCurrentCandidate();
    window.location.href = "/login";
  }
}

export async function apiRequest<T>(path: string, init?: RequestInit): Promise<T> {
  const response = await fetch(`${API_BASE_URL}${path}`, {
    headers: {
      "Content-Type": "application/json",
      ...resolveAuthHeaders(path),
      ...init?.headers,
    },
    ...init,
  });

  if (!response.ok) {
    if (response.status === 401) {
      handle401(path);
    }
    throw await parseError(response);
  }

  const body = (await response.json()) as ApiResponse<T>;
  return body.data;
}

export async function uploadRequest<T>(path: string, file: File): Promise<T> {
  const formData = new FormData();
  formData.append("file", file);
  const response = await fetch(`${API_BASE_URL}${path}`, {
    method: "POST",
    body: formData,
    headers: {
      ...resolveAuthHeaders(path),
    },
  });
  if (!response.ok) {
    if (response.status === 401) {
      handle401(path);
    }
    throw await parseError(response);
  }
  const body = (await response.json()) as ApiResponse<T>;
  return body.data;
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd frontend && npx vitest run src/api/client.test.ts
```

预期：5 tests PASS

- [ ] **Step 5: 提交**

```bash
git add frontend/src/api/client.ts frontend/src/api/client.test.ts
git commit -m "feat: apiRequest/uploadRequest 自动注入认证 header + 401 跳转"
```

---

### Task 3: 修改 `AdminLoginPage` — 登录成功后保存 token

**Files:**
- Modify: `frontend/src/pages/admin/AdminLoginPage.tsx`

- [ ] **Step 1: 添加 import**

在文件顶部 import 区添加：

```typescript
import { setAdminToken } from "@/lib/adminSession";
```

- [ ] **Step 2: 修改 onSuccess 回调**

将第 30 行：

```typescript
onSuccess: () => navigate("/admin/dashboard"),
```

改为：

```typescript
onSuccess: (_data, variables) => {
  setAdminToken(variables.password);
  navigate("/admin/dashboard");
},
```

- [ ] **Step 3: 运行 lint 确认无报错**

```bash
cd frontend && npx eslint src/pages/admin/AdminLoginPage.tsx
```

预期：无错误

- [ ] **Step 4: 提交**

```bash
git add frontend/src/pages/admin/AdminLoginPage.tsx
git commit -m "feat: AdminLoginPage 登录成功后保存 token 到 localStorage"
```

---

### Task 4: 修改 `AdminLayout` — 登录态守卫

**Files:**
- Modify: `frontend/src/components/layout/AdminLayout.tsx`

- [ ] **Step 1: 添加 import**

在文件顶部添加：

```typescript
import { useEffect } from "react";
import { useNavigate } from "react-router-dom";
import { getAdminToken } from "@/lib/adminSession";
```

- [ ] **Step 2: 添加守卫逻辑**

在 `AdminLayout` 函数体顶部（`return` 之前）添加：

```typescript
const navigate = useNavigate();

useEffect(() => {
  if (!getAdminToken()) {
    navigate("/admin/login", { replace: true });
  }
}, [navigate]);
```

- [ ] **Step 3: 运行 lint 确认无报错**

```bash
cd frontend && npx eslint src/components/layout/AdminLayout.tsx
```

预期：无错误

- [ ] **Step 4: 提交**

```bash
git add frontend/src/components/layout/AdminLayout.tsx
git commit -m "feat: AdminLayout 添加登录态守卫，无 token 时跳转登录页"
```

---

### Task 5: 修改 `startExam` — 去掉 body 中的 candidate_id

**Files:**
- Modify: `frontend/src/api/exams.ts:31-36`
- Modify: `frontend/src/pages/ExamStartPage.tsx:27`

- [ ] **Step 1: 修改 `startExam` 函数签名和 body**

将 `frontend/src/api/exams.ts` 第 31-36 行：

```typescript
export function startExam(examId: string, candidateId: number) {
  return apiRequest<ExamStartResponse>(`/api/exams/${examId}/start`, {
    method: "POST",
    body: JSON.stringify({ candidate_id: candidateId }),
  });
}
```

改为：

```typescript
export function startExam(examId: string) {
  return apiRequest<ExamStartResponse>(`/api/exams/${examId}/start`, {
    method: "POST",
  });
}
```

- [ ] **Step 2: 修改调用方**

将 `frontend/src/pages/ExamStartPage.tsx` 第 27 行：

```typescript
mutationFn: () => startExam(examId, candidate?.id ?? 0),
```

改为：

```typescript
mutationFn: () => startExam(examId),
```

- [ ] **Step 3: 运行类型检查确认无报错**

```bash
cd frontend && npx tsc --noEmit
```

预期：无错误

- [ ] **Step 4: 提交**

```bash
git add frontend/src/api/exams.ts frontend/src/pages/ExamStartPage.tsx
git commit -m "fix: startExam 去掉 body 中的 candidate_id，改由 header 注入"
```

---

### Task 6: 全量验证

- [ ] **Step 1: 运行全部测试**

```bash
cd frontend && npx vitest run
```

预期：全部 PASS（含新增的 adminSession 和 client 测试）

- [ ] **Step 2: 运行 lint**

```bash
cd frontend && npx eslint src/
```

预期：无错误

- [ ] **Step 3: 运行 build**

```bash
cd frontend && npm run build
```

预期：构建成功，无类型错误

- [ ] **Step 4: 运行 format check**

```bash
cd frontend && npx prettier --check src/
```

预期：全部文件格式正确（如有不符则 `npx prettier --write` 修复后重新提交）

- [ ] **Step 5: 最终提交（如有格式修复）**

```bash
git add -u
git commit -m "chore: 代码格式化"
```
